{
  "name": "reporter",
  "version": "0.1.0",
  "description": "AI-powered daily work report generator",
  "type": "module",
  "bin": {
    "reporter": "src/index.ts"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "link": "bun link"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "smol-toml": "^1.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
